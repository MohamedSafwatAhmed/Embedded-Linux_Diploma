#include <iostream>
#include <ostream>

int main(void)
{   int x,y,z;
    std::cout<<"Enter the Legs:"<<std::endl;
    std::cin>>x>>y;
    std::cout<<"Enter the hgohooh:";
    std::cin>>z;
    if( (z*z)==(x*x +y*y) )
        std::cout<<"IT's right angle Traingle"<<std::endl;
    else
            std::cout<<"It not right angle Traingle"<<std::endl;
        
    

}